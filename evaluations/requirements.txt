# =============================================================================
# EVALUATION EXTRAS (DEPRECATED)
# =============================================================================
# All evaluation dependencies are now in the root requirements.txt.
# This file is kept for backward compatibility with CI pipelines that
# may reference it. The unified environment uses a single venv.
# =============================================================================
# Primary dependencies (now in root requirements.txt):
#   azure-ai-evaluation[redteam]==1.12.0
#   pandas
#
# If you need to install eval-only extras separately:
# pip install azure-ai-evaluation[redteam]==1.12.0 pandas
