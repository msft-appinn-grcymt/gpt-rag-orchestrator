# Azure SDK dependencies
azure-keyvault-secrets==4.8.0
azure-cosmos==4.5.1
azure-identity==1.21.0
azure-mgmt-web==8.0.0
azure-mgmt-storage==22.1.1
azure-storage-blob==12.25.1
azure-appconfiguration==1.7.1
azure.appconfiguration.provider
azure-ai-projects>=2.0.0
azure-search-documents==11.7.0b1

# AI and NLP dependencies
tiktoken==0.7.0
openai>=1.108.0

# Semantic Kernel sub-dependencies
# NOTE: semantic-kernel>=1.40.0 is installed separately with --no-deps
# because it pins azure-ai-projects~=1.0.0b12 as a core dependency,
# which conflicts with azure-ai-projects>=2.0.0. Our code only uses
# ChatCompletionAgent, GroupChatOrchestration, Kernel, AzureChatCompletion,
# MCPSsePlugin, and kernel_function — none of which need azure-ai-agents.
mcp>=1.8
pydantic-settings>=2.0
aiohttp==3.12.14
pydantic==2.11.4

# Utility dependencies
tenacity

# For evaluation
requests==2.32.4
python-dotenv==1.1.0

# fastapi
fastapi==0.115.12
uvicorn==0.34.1

# NL2SQL dependencies
sqlparse==0.5.1
pyodbc==5.1.0
#opentelemetry
azure-monitor-opentelemetry
azure-monitor-opentelemetry-exporter
opentelemetry-instrumentation-httpx==0.52b1
opentelemetry-instrumentation-fastapi

# Evaluation dependencies
azure-ai-evaluation[redteam]==1.12.0
pandas